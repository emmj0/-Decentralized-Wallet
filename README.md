# Decentralized Cryptocurrency Wallet System

Project skeleton for the final project: Decentralized Cryptocurrency Wallet System with a custom blockchain (UTXO, PoW, digital signatures), Go backend, React + Tailwind frontend, and serverless DB.

Work in progress — initial scaffold created.

Next steps:
- Implement Go backend modules (blockchain, utxo, miner, api)
- Scaffold React + Tailwind frontend pages
- Integrate serverless DB (Firebase / MongoDB Atlas / Supabase)
- Implement auth, key management, zakat scheduler, logging, and reports
- Deploy frontend (Vercel) and backend (Render/Fly.io) and record demo

See `backend/README.md` and `frontend/README.md` for per-component instructions.
